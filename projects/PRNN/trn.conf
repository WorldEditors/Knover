PROJECT_DIR=/mnt/wangfan/worldEditors/Knover/projects/PRNN
ROOTPATH=/mnt/wangfan/worldEditors/Knover

# job settings
job_script="$ROOTPATH/scripts/distributed/train.sh"

# task settings
model=UnifiedTransformer
task=DialogGeneration

vocab_path="$ROOTPATH/package/dialog_en/vocab.txt"
spm_model_file="$ROOTPATH/package/dialog_en/spm.model"
train_file="$ROOTPATH/data/example/train_wow.tokenized.tsv"
valid_file="$ROOTPATH/data/example/val_wow_seen_unseen.tokenized.tsv"
data_format="tokenized"
#raw, tokenized, numerical
file_format="file"
config_path="$ROOTPATH/projects/PRNN/trn.json"

# training settings
in_tokens="true"
batch_size=8192
lr=1e-5
warmup_steps=500
weight_decay=0.01
num_epochs=10000

train_args="--max_src_len 0 --max_seq_len 1024"

log_steps=10
validation_steps=1000
save_steps=10000

log_dir="$PROJECT_DIR/results/logs.trn12"
save_path="$PROJECT_DIR/results/outputs.trn12"
